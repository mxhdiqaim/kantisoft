{
  "name": "kantisoft",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo dev --parallel",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.8.15"
  },
  "packageManager": "bun@1.3.5",
  "workspaces": ["apps/*", "packages/*"]
}
